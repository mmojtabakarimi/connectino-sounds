#!/bin/sh
# Copyright 2020 Benoît Stahl
# SPDX-License-Identifier: GPL-3.0+

case "$1" in
    build)
        ;;

    package)
  mkdir -p ${pkgdir}/etc/systemd/system
        ;;

    install)
      wget 
  mkdir /tmp/wazo-sounds
        ;;

    uninstall)
        ;;

    *)
        echo "$0 called with unknown argument '$1'" >&2
        exit 1
    ;;
esac
